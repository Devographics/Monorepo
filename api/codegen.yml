overwrite: true
schema:
  - src/type_defs/*.graphql:
      skipGraphQLImport: true # skip #import comments
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      contextType: ../types#RequestContext
