
input Options {
  limit: Int
  cutoff: Int
  sort: SortSpecifier
  facetSort: SortSpecifier
  facetLimit: Int
  facetMinPercent: Float
  facetMinCount: Int
}

input SortSpecifier {
  property: String
  order: SortOrder
}

enum SortOrder {
  asc
  desc
}