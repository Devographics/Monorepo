id: ${mainBlockId}_vs_salary
fieldId: ${mainBlockId}
# i18nNamespace: ${mainBlockId}
# tabId: tabs.by_salary
# defaultUnits: percentilesByFacet
blockType: HorizontalBarBlock2
hasData: true
parameters:
    showNoAnswer: true
    cutoff: 10
    # sort: { property: medianByFacet }
filtersState:
    facet:
        sectionId: user_info
        id: yearly_salary
