blockType: PicksBlock
i18nNamespace: picks
wrapBlock: false
disableExport: true
dataPath: 'dataAPI.entity_${questionId}'
query: >
    entity_${questionId}: entity(id: "${questionId}") {
      id
      homepage {
        name
        url
      }
      name
      nameClean
      twitter {
        name
        url
      }
    }
