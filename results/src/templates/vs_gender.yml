id: ${mainBlockId}_vs_gender
fieldId: ${mainBlockId}
# i18nNamespace: ${mainBlockId}
# tabId: tabs.by_salary
# defaultUnits: percentilesByFacet
blockType: HorizontalBarBlock2
hasData: true
parameters:
    showNoAnswer: true
filtersState:
    options:
        mode: facet
    facet:
        sectionId: user_info
        id: gender
