id: ${mainBlockId}_vs_age
fieldId: ${mainBlockId}
# i18nNamespace: ${mainBlockId}
# tabId: tabs.by_salary
# defaultUnits: percentilesByFacet
blockType: HorizontalBarBlock2
hasData: true
parameters:
    showNoAnswer: true
    cutoff: 20
filtersState:
    options:
        mode: facet
    facet:
        sectionId: user_info
        id: age
