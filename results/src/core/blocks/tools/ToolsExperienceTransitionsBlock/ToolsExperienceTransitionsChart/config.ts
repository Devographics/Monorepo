import { CustomSankey } from './CustomSankey'

export const staticProps = {
    layers: [CustomSankey],
    margin: {
        top: 30,
        right: 10,
        bottom: 10,
        left: 10,
    },
}