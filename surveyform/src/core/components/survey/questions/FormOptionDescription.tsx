import React from "react";

const FormOptionDescription = ({ optionDescription }) => {
  return <span className="form-option-description">{optionDescription}</span>;
};

export default FormOptionDescription;
