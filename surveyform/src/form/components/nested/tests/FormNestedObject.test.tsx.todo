import React from "react";
// TODO: should be loaded from Components instead?
import { render, fireEvent, waitFor, screen } from "@testing-library/react";
import { FormNestedObject } from "../FormNestedObject";

// helpers
// tests
describe("vulcan-forms/FormNestedObject", function () {
  const defaultProps = {
    errors: [],
    path: "foobar",
  };
  it.skip("shallow render", function () {
    /*
    const wrapper = render(
      <FormNestedObject {...defaultProps} currentValues={{}} />
    );
    expect(wrapper).toBeDefined();
    */
  });
  it.skip('render a Form collectionName="" for the object', function () {
    /*
    // eslint-disable-next-line no-unused-vars
    const { container } = render(
      <FormNestedObject {...defaultProps} currentValues={{}} />
    );
    expect(false).toBe(true);
    */
  });
  it.skip("does not show any button", function () {
    /*
        const {container} = render(<Components.FormNestedObject {...defaultProps} currentValues={{}} />);
        const button = wrapper.find('BootstrapButton');
        expect(button).toHaveLength(0);
        */
  });
  it.skip("does not show add button", function () {
    /*
        const {container} = render(<Components.FormNestedObject {...defaultProps} currentValues={{}} />);
        const addButton = wrapper.find('IconAdd');
        expect(addButton).toHaveLength(0);
        */
  });
  it.skip("does not show remove button", function () {
    /*
        const {container} = render(<Components.FormNestedObject {...defaultProps} currentValues={{}} />);
        const removeButton = wrapper.find('IconRemove');
        expect(removeButton).toHaveLength(0);
        */
  });
});
