import { tokens as headerTokens } from "~/components/common/Header.tokens"
import { tokens as footerTokens } from "~/components/common/Footer.tokens"
export const tokens = ["general.skip_to_content", ...headerTokens, ...footerTokens] as const